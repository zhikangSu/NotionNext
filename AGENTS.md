# Project Instructions

## Git Sync Boundaries

- This checkout is the user's personal NotionNext site. The primary remote is `origin` (`zhikangSu/NotionNext`) and the working branch is `main`.
- Do not casually fetch, merge, rebase, or otherwise sync the official NotionNext upstream. The user has many local/personal improvements here, and official upstream changes should only be brought in when the user explicitly asks for that specific sync.
- For ordinary site updates, inspect and update the local code, commit to `main`, and push to `origin/main`.
- If an upstream sync is explicitly requested, first check the working tree, summarize the expected conflict risk, and preserve personal configuration and site-specific changes.
